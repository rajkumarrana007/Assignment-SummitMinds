#Assignment: Using Python

import json

data = json.load(open("prize.json"))
#print(data)

for i, j in data.items();
	for index, data in enumerate(j);
	data = data
	year = input("Enter Year :")
	if data["year"] == year;
	print(data)
	continue
